import React from 'react';
type FastProperties = {
    readonly singleLine?: boolean;
    readonly upload?: boolean;
    readonly json?: boolean;
};
declare const Fast: React.FC<FastProperties>;
export default Fast;
